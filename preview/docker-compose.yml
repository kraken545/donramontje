services:
  donramontje-preview:
    image: nginx:1.27-alpine
    container_name: donramontje-preview
    ports:
      - "8080:80"
    volumes:
      - ../site:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
